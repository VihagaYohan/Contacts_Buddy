import React, {Component} from 'react';
import {StyleSheet} from 'react-native';

// components
import {UIContainer, UITextView} from '../components';

const HomeScreen = () => {
  return (
    <UIContainer>
      <UITextView text="Details page" />
    </UIContainer>
  );
};

const styles = StyleSheet.create({});

export default HomeScreen;
